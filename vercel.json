{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter frontend build",
  "outputDirectory": "apps/frontend/.next",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
